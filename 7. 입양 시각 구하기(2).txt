SELECT B.HOUR, count(A.ANIMAL_ID) AS COUNT
FROM ANIMAL_OUTS A, (SELECT LEVEL-1 AS HOUR
                     FROM DUAL
                     CONNECT BY LEVEL BETWEEN 1 AND 24) B
WHERE TO_CHAR(A.DATETIME(+),'HH24') = B.HOUR
GROUP BY B.HOUR
ORDER BY B.HOUR;